export default class SecurityContextsNotEnabledError extends Error {
  constructor() {
    super("Security Contexts selection must be enabled when calling a 3DSpace webservice that needs one.");
  }

}